<header class="site-header">
  <div class="wrap">
    <a class="site-title" href="{{ '/' | siteLink(page.url) }}">{{ site.title }}</a>
    <nav class="site-nav">
      <div class="trigger">
        <a class="page-link" href="{{ '/about/' | siteLink(page.url) }}">About</a>
        <a class="page-link" href="{{ '/feed.xml' | siteLink(page.url) }}">RSS</a>
      </div>
    </nav>
  </div>
</header>
